
name: Build Akanksha APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Extract project ZIP
        run: |
  if [ -f "AKANKSHA.APK" ]; then
    unzip -o "AKANKSHA.APK"
  elif [ -f "AkankshaChat_AndroidStudio.zip" ]; then
    unzip -o "AkankshaChat_AndroidStudio.zip"
  else
    echo "Project file not found"
    exit 1
  fi

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Android
        uses: android-actions/setup-android@v3

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'

      - name: Build APK
        run: gradle assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Akanksha-APK
          path: app/build/outputs/apk/debug/app-debug.apk
